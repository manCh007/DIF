# consolidate — project-specific overrides

This file is read by the `consolidate` skill on every run, after the defaults in `SKILL.md`. Override it at the project level via `.claude/skills/consolidate/rules.md` if you'd rather not fork this plugin (see SETUP.md §5).

<!--
## Consolidated-diff format preference
(e.g. "organize by file, not by task"; "include a risk callout section")
- 

## Things that always belong in the decision log
(beyond plan deviations — e.g. any change to a public API contract)
- 
-->
