# plan — project-specific overrides

This file is read by the `plan` skill on every run, after the defaults in `SKILL.md`. Override it at the project level via `.claude/skills/plan/rules.md` if you'd rather not fork this plugin (see SETUP.md §5).

<!--
## Task granularity preference
(e.g. "prefer one task per file" vs "prefer one task per logical change
spanning several files")
- 

## Modules that always deserve their own task
(e.g. database migrations, public API contracts)
- 

## Dependency conventions specific to this repo
(e.g. "schema changes always depend-block application-code tasks in the same
plan")
- 
-->
